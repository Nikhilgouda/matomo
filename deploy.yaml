apiVersion: "v1"
kind: "Service"
metadata:
  name: "matomo1-service"
  namespace: "default"
  labels:
    app: "matomo1"
spec:
  ports:
  - protocol: "TCP"
    port: 80
  selector:
    app: "matomo1"
  type: "LoadBalancer"
  loadBalancerIP: ""
